// Redirected to the global layout widget.
// Keep this file so existing imports don't break.
export 'package:nutritrack/presentation/layout/app_sidebar.dart'
    show AppSidebar;

/// Alias kept for backwards compatibility.
typedef ClientsSidebar = AppSidebar;
