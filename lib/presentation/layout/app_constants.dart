import 'package:flutter/material.dart';

// ── Background / surface ──────────────────────────────────────────────────────
const Color appBgColor = Color(0xFFF8F8F6);

// ── Border ────────────────────────────────────────────────────────────────────
const Color appBorderColor = Color(0xFFE2E2DD);
const Color appInputBorderColor = Color(0xFFE3E3DE);

// ── Brand / accent ────────────────────────────────────────────────────────────
const Color appBrandColor = Color(0xFF0FA37F);
const Color appSelectedBgColor = Color(0xFFDCEFE9);

// ── Text / icon ───────────────────────────────────────────────────────────────
const Color appMutedTextColor = Color(0xFF7A7A73);
const Color appSecondaryIconColor = Color(0xFF6F6F68);
const Color appBodyTextColor = Color(0xFF4D4D47);
const Color appHeadingColor = Color(0xFF20201D);

// ── Border radii ──────────────────────────────────────────────────────────────
const BorderRadius appBorderRadius = BorderRadius.all(Radius.circular(10));
const BorderRadius appChipBorderRadius = BorderRadius.all(Radius.circular(8));

// ── Layout dimensions ─────────────────────────────────────────────────────────
const double appTopBarHeight = 74;
const double appButtonHeight = 40;
const double appIconSize = 18;
const double appSidebarExpandedWidth = 222;
const double appSidebarCollapsedWidth = 72;
